{
  "version": 2,
  "builds": [
    {
      "buildCommand": "prisma generate && prisma migrate deploy && npm run build",
      "src": "./src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/src/index.js"
    }
  ]
}
